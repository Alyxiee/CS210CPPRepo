9/14/2026 8:53PM
- I chose to make `topIndex` initialize as `-1` so that I could have it represent the current top index rather than representing the current stack size, which would occur if I initialized it as 0.
- I declared the functions in the `Stack.h` file and defined the functions in `Stack.cpp`

9/14/2026 9:16PM
- Finally implemented every declared function and made sure they work using the calls in `main.cpp`
- Decided that full behavior should just ignore rather than overwrite.
- Decided that empty behavior should just return that the stack is empty.
- For the above, I just found it easier to ignore the attempted push if the stack is full. Less time on homework, the better.

9/14/2026 9:37PM
- Wrapping up the files and doing the `Devlog` and `ReadMe`

9/14/2026 10:24PM
- Finally done.